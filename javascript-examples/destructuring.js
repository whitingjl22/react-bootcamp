// Destructuring

const obj = {
	name: 'Joe',
	food: 'cake'
}
const { name, food } = obj;
console.log(name, food);
// 'Joe' 'cake'



let jeremyObject = {
	firstName: 'Jeremy',
	city: 'Milwaukee',
	beer: 'IPA'
}

let { firstName, city, beer } = jeremyObject
console.log(firstName, city, beer)

