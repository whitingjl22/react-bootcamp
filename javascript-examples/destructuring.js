// Destructuring

const obj = {
  name: "Joe",
  food: "cake"
}
const { name, food } = obj
console.log(name, food)
// 'Joe' 'cake'

const jeremyObject = {
  firstName: "Jeremy",
  city: "Milwaukee",
  beer: "IPA"
}

const { firstName, city, beer } = jeremyObject
console.log(firstName, city, beer)
